export  enum Path {
    Layout = "/",
    About = "/about",
}
export enum StoreKey {
    Home = "home-search",

}

export enum SlotID {
    AppBody = "app-body",
    CustomModel = "custom-model",
  }
  